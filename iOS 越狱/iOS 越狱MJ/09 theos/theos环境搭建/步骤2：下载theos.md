```swift
//这里的THEOS是在步骤1配置的全局使用参数
sudo git clone --recursive [https://github.com/theos/theos.git](https://github.com/theos/theos.git) $THEOS

//或者（下面这个操作来自网上，上面的无效时使用）
sudo git clone --recursive https://github.com/theos/theos.git /opt/theos
```